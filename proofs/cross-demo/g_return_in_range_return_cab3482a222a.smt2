; auto-generated by cpg.proof for function g
; goal: ReturnInRangeClaim(regime='witness', enforcement='trust', justification=None, kind='return_in_range', min=-1, max=None)
(set-logic QF_UFLIA)

(declare-const y Int)
(assert (>= y -2147483648))
(assert (<= y 2147483647))

; callee declarations (cross-procedural)
(declare-fun f (Int) Int)

; hypotheses (callees' return claims, applied per call)
;   f's return_in_range return claim, on (f y)
(assert (>= (f y) -1))

; goal (negated; we ask Z3 to find a counterexample)
(assert (not (>= (ite (< (f y) -100) 999 (f y)) -1)))

(check-sat)
(exit)
